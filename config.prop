myuser=andres
myhostname=archie

lang=en_US.UTF-8
loc=US/Eastern

rootDir=/mnt
homeDir=/home
bootDir=/boot
userDir=${homeDir}/${myuser}

device=/dev/sda
partBoot=${device}1
partRoot=${device}2
partHome=${device}3
swapfile=/swapfile
swapSize=1024M
rootSize=50G

bootloader=1
bootloaderType=efi
network=wifi

vm=0
