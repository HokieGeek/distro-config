myuser=andres
rootDir=/mnt
homeDir=/home
bootDir=/boot
userDir=${homeDir}/${myuser}

device=/dev/sda
part_boot=${device}1
part_root=${device}2
part_home=${device}3
swapfile=/swapfile

vm=0
bootloader=1
bootloaderType="bios"
network="wifi"
