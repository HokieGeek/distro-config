myuser=andres
myhostname=MiJugeteFavorito
rootDir=/mnt
homeDir=/home
bootDir=/boot
userDir=${homeDir}/${myuser}

device=/dev/sda
partBoot=${device}1
partRoot=${device}2
partHome=${device}3
swapfile=/swapfile
swapSize=1024M
rootSize=100G

vm=0
bootloader=1
bootloaderType="bios"
network="wifi"
